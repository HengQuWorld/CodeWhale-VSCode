# Source code (already bundled into dist/extension.js by webpack)
src/**
node_modules/**

# Build configuration (not needed at runtime)
tsconfig.json
webpack.config.js
vitest.config*

# Test infrastructure
.vitest/**
coverage/**
*.profraw

# Documentation (not needed at runtime)
AGENTS.md
design-language.md
debug-ide-plugin-freeze.md
troubleshooting-ide-plugin-freeze.md
# README_zh.md is GitHub-only (the marketplace reads README.md)
README_zh.md

# Repo infrastructure (not needed at runtime)
.github/**
.editorconfig
eslint.config.mjs
CONTRIBUTING.md
SECURITY.md
CODE_OF_CONDUCT.md

# Docs and demo media (guides + recordings — never shipped; keeps the VSIX ~200 KB)
docs/**

# Package lock & gitignore (not needed at runtime)
package-lock.json
.gitignore

# Source maps (save ~50% dist/ space)
dist/*.map
.trae/**
.codewhale/**
